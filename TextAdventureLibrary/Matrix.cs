namespace TextAdventureLibrary
{
    public class Matrix
    {
        public Cell[,] matrix;

        public Matrix(Cell[,] matrix)
        {
            this.matrix = matrix;
        }

        public Matrix(MapKey key, string map)
        {
            //split string by returns
            //assign cell to matrix based on mapKey
        }
    }
}
